# ملخص جلسة 12 أغسطس 2026 — نظام صور المقالات (الإصدار النهائي)

## 1. الميزات المضافة

### كاروسال مقالات الأسماء في قسم الحمل
- ملف جديد: `lib/widgets/names_articles_carousel.dart`
  - `NamesArticlesCarousel` — شريط أفقي (بطاقات 190×226) يعرض 12 مقالاً من `namesArticlesDatabase` بتناوب التصنيفات
  - شارات ملوّنة: شرعية = تركوازي • علم النفس = بنفسجي • غيرها = برتقالي
  - `NameArticleDetailScreen` — شاشة تفاصيل (SliverAppBar 220)
  - `_AllNamesArticlesScreen` — «عرض الكل» مع فلتر ChoiceChip حسب التصنيف
- أُدرج في `pregnancy_weeks_screen.dart` بعد `_buildArticlesCarousel()` → **يظهر في كل الأسابيع**

## 2. إصلاح نظام الصور (السبب الجذري)

كانت الشاشات تستعمل `Image.network` / `Image.asset` مباشرةً بدل `ArticleImage`، وبعضها بشرط:

```dart
art.image.startsWith('http') ? ArticleImage(...) : Image.asset(art.image)   // ← الخطأ
```

فالمقالات ذات الرابط القديم (Unsplash) كانت تأخذ الصورة الجديدة، والمقالات ذات الملف المحلي (`pregnancy_news/*.jpg`) تبقى على القديم — وهذا سبب «الخليط» بين الداخل والخارج.

### الملفات المعدّلة
| الملف | التعديل |
|---|---|
| `lib/widgets/news_section.dart` | صورتا القائمة والبطاقة → `ArticleImage` |
| `lib/main.dart` | **نسخة ثانية** من `_NewsSection` (أخبار الصفحة الرئيسية) — صورتان → `ArticleImage` |
| `lib/widgets/conditional_content.dart` | 3 مواضع (بطاقة، صف صغير، رأس المقال) → `ArticleImage` — هذا ما أعاد صور **كل الأقسام المتخصصة** (توأم، سكري الحمل، ضغط، غدة، خدّج، قيصرية…) |
| `lib/screens/pregnancy/pregnancy_weeks_screen.dart` | 5 مواضع: كاروسال الأقسام (1503)، بطاقة أفقية (1406)، بطاقة الأخبار (959)، رأس شاشة التفاصيل (2903)، صف المقال (3203) |
| `lib/utils/article_images.dart` | `preload()` صار يحمّل `news_images.json` أيضاً ويدمجها في `_titleToId` |
| `lib/utils/article_image_map.g.dart` | **حُذفت 10 روابط تلقائية خاطئة** كانت تسرق صور الأخبار (a359، a361–a365، a367، a372، a375، a380) لمقالات الطفل |
| `pubspec.yaml` | إضافة `assets/data/news_images.json` |

### ملف الربط الإضافي `assets/data/news_images.json`
يغطّي ما لا تغطّيه الخريطة المولّدة:
- **6 مرادفات** لعناوين أخبار اختلف نصّها قليلاً عن الخريطة (تماماً / تركيبه / في الحضانة / دراسة: / أصغر)
- **10 أخبار** → صورها الصحيحة `a359, a361–a365, a367, a372, a375, a380`
- **10 مقالات** فقدت صورها بعد التصحيح → `b001`–`b010`
- **7 أخبار Firestore** → `f001`–`f007`
- **30 مقال الأقسام الخمسة** → `d001`–`d030`

## 3. التحقق من Firestore
مجموعة `dynamic_articles` تحتوي **7 مقالات** فقط (قسم news)، كلها بصور Pexels:
قصة الجنين المتحجر • خلايا طفلك تسكن قلبكِ • معجزة الزمن (جنين مجمد 27 عاماً) • تحذير: الولادة الحرة • بطلة في زمن الحرب • مفاجأة: التهاب اللثة والخصوبة • أسرار حديثي الولادة.
قُرئ نصّ كل مقال وكُتب له برومبت مطابق → `f001`–`f007`.

## 4. الصور المولّدة في هذه الجلسة (47 صورة)
- `d001`–`d030` — أخبار الحمل الحديثة، دراسات وأبحاث، تقنيات حديثة في المتابعة، صحة الأم والجنين، ما بعد الولادة
- `f001`–`f007` — أخبار Firestore
- `b001`–`b010` — المقالات التي فقدت صورها بعد تصحيح ربط الأخبار
- `a031` + إصلاح اسم `a300.png.png` → `a300.png`

**النتيجة: لا يوجد مقال بلا صورة.**

## 5. ملفات مساعدة أُنشئت
- `برومبتات_الصور_الناقصة_40.md` — كل البرومبتات (47) مع أسماء الملفات
- `برومبتات_الأخبار_31.md` — برومبتات أخبار الرئيسية (مرجع)
- `compress_new_images.py` + `compress_new_images.bat` — ضغط 900px / JPEG q82 داخل امتداد png
- `run_nabda.bat` — ضغط ← clean ← pub get ← run
- `sync_and_push_all.bat` — مزامنة OneDrive + D: ثم pull --rebase و push

## 6. الخطوة التالية
`build_and_deploy_web.bat` لنشر الموقع بعد التأكد من التطبيق.
