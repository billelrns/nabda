# تفعيل إعلانات جوجل (AdMob) في نبضة

النظام مبنيّ وجاهز — يعمل الآن بإعلاناتك الخاصة، ويتحوّل إلى AdMob بخطوات بسيطة متى فتحتِ الحساب.

## كيف يعمل الترتيب داخل كل مساحة إعلانية

```
① إعلان AdMob        ← إن كان مفعّلاً وتوفّر إعلان
② إعلان نبضة الخاص   ← من مجموعة ads في Firestore
③ منتج من المتجر     ← مطابق تلقائياً لموضوع المقال
```

فلا تبقى المساحة فارغة أبداً.

## المواضع الإعلانية داخل المقال
| الموضع | المكان |
|---|---|
| slot 0 | بعد الفقرة الثانية |
| slot 1 | منتصف المقال (للمقالات الطويلة فقط) |
| slot 2 | نهاية المقال |

كل مساحة تحمل شارة **«إعلان»** صغيرة رمادية — مطلوبة في سياسات جوجل وتزيد ثقة المستخدمة.

---

## خطوات التفعيل (5 دقائق)

### ١. افتحي حساب AdMob
[admob.google.com](https://admob.google.com) ← أضيفي التطبيق ← احصلي على:
- **App ID** بالشكل `ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY`
- **Ad Unit ID** لبانر بالشكل `ca-app-pub-XXXXXXXXXXXXXXXX/AAAAAAAAAA`

### ٢. أضيفي الحزمة
في `pubspec.yaml` تحت `dependencies:`
```yaml
  google_mobile_ads: ^5.2.0
```
ثم `flutter pub get`

### ٣. AndroidManifest
`android/app/src/main/AndroidManifest.xml` داخل وسم `<application>`:
```xml
<meta-data
    android:name="com.google.android.gms.ads.APPLICATION_ID"
    android:value="ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY"/>
```

### ٤. في `lib/services/admob_service.dart`
- ضعي معرّفاتك في `androidBannerId` و `iosBannerId`
- غيّري `enabled = false` إلى `true`
- أزيلي التعليق عن كل الأسطر المعلَّمة بـ `// ADMOB:`

### ٥. أعيدي البناء
```
flutter clean && flutter pub get && flutter run
```

> في وضع التطوير (debug) يستعمل الكود **معرّفات الاختبار الرسمية** من جوجل تلقائياً، فلا خطر على حسابك من نقرات التجربة.

---

## مطابقة المنتجات بموضوع المقال

في `lib/widgets/news_section.dart` ← `NabdaAds.pickProductFor()`

يستخرج مواضيع المقال من نصّه ثم يعطي كل منتج نقاطاً:

| الموضوع | يظهر في المقال | يُطابَق بمنتجات |
|---|---|---|
| رضاعة طبيعية | رضاعة، حليب، ثدي، شفط | مضخة، وسادة رضاعة، شفاط |
| رضاعة صناعية | ببرونة، زجاجة، تعقيم | رضّاعة، معقم، حلمة |
| حفاضات | حفاض، طفح، تغيير | حفاض، مناديل، كريم |
| نوم | نوم، سرير، أرق، قيلولة | مهد، كيس نوم، هزاز |
| تغذية | طعام، فطام، وجبات | محضّرة، كرسي طعام، مريلة |
| فيتامينات | حديد، فوليك، كالسيوم | مكمّلات، حبوب |
| بشرة | تشقق، ترطيب، علامات تمدد | كريم، زيت، مرطب |
| متابعة طبية | سونار، ضغط، سكري، قياس | جهاز، ترمومتر، دوبلر |
| تحضير الولادة | حقيبة، مخاض، قيصرية | طقم ولادة، فوطة |
| عناية بالطفل | استحمام، تسنين، مغص | حوض، عضاضة، شامبو |
| عربات وتنقّل | سفر، تنقل | عربة، كرسي سيارة، حمالة |
| رياضة | تمارين، يوغا، كيغل | كرة، سجادة، مطاط |

**لتحسين الدقة:** أضيفي حقل `tags` (مصفوفة نصوص) لكل منتج في `dynamic_products` — يُعطى وزناً أعلى في المطابقة.

مثال:
```json
{ "name": "مضخة حليب كهربائية", "tags": ["breastfeeding", "رضاعة", "مضخة"] }
```

---

## ما أُزيل
كل الصور الجاهزة القديمة (Unsplash) داخل نصوص المقالات — 17 مجموعة صور. الآن تظهر داخل المقال **فقط** الصور التي تضيفينها أنتِ من لوحة التحكم لذلك المقال تحديداً.
